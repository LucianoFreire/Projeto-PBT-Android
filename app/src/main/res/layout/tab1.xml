<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin">

<!--####################################################################-->
    <!-- Faixa Superior -->
    <FrameLayout
        android:layout_width="match_parent"
        android:layout_height="262dp"
        android:layout_weight="1.03"
        android:background="@drawable/img2">

        <!-- BOTÃO LEGISLAÇÃO -->
        <ImageButton
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginTop="90dp"
            android:id="@+id/btn_legislacao"
            android:background="#ffffff"
            android:src="@drawable/btnlegislacaon"
            android:onClick="legislacao"/>

        <!-- BOTÃO DEFINIÇÃO -->
        <ImageButton
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginTop="30dp"
            android:id="@+id/btn_definicao"
            android:background="#ffffff"
            android:src="@drawable/btn_defincao"
            android:onClick="definicao"/>

    </FrameLayout>

<!--##################################################################-->

    <!--Caixa de texto superior PBT RODOVIARIO
    <LinearLayout
        android:orientation="vertical"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:background="#727273"
        android:gravity="center"
        android:paddingTop="0dp">

         Texto PBT Rodoviario
        <TextView
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="PBT RODOVIÁRIO"
            android:textSize="20dp"
            android:textColor="#050505"
            android:textAlignment="center"
            android:textStyle="bold"
            android:id="@+id/PbtRod"/>
    </LinearLayout>
    -->
<!--##################################################################-->

    <!-- Faixa Inferior
    <LinearLayout
        android:orientation="vertical"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:background="#2b2b2d"
        android:gravity="center"
        android:paddingBottom="40dp"
        >
    </LinearLayout>
    -->

</LinearLayout>
